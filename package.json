{
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "postinstall": "husky",
    "format": "pnpm -r format",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "husky": "9.1.7"
  }
}
